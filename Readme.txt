Fancy specifics:
-using pikcup "zones" to handle pickup collision checks more efficient
-able to load in all level items from .csv file
-using pools for rendering and pickups
-input objects (commands) to make it easy to map keys)

I used source tree but I have no idea where to retrieve a link for that